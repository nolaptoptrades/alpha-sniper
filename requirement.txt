# NLT Alpha Sniper — dependencies
# Python 3.10+ required

# HTTP requests (Telegram bridge bot)
requests>=2.28.0

# Environment variable loading
python-dotenv>=1.0.0

# Terminal UI
rich>=13.0.0

# Windows curses support (Windows only)
windows-curses>=2.3; sys_platform == "win32"
